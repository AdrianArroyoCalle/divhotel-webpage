function trial(json)
{
	//This function makes avalible the download button for downloading the trial version of DivHotel
	var trial=document.getElementById("trial");
	trial.textContent="Currently the trial is not avalible. Check later";
}